<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vivo.game">

    <!-- Original permissions -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ storage -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Needed to launch apps -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@drawable/game_app_icon"
        android:label="@string/game_app_name"
        android:name=".GameApplication"
        android:supportsRtl="true"
        android:theme="@style/game_theme">

        <!-- ============================================================ -->
        <!--  ORIGINAL ACTIVITIES (kept as-is)                            -->
        <!-- ============================================================ -->
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:label="@string/game_app_name"
            android:launchMode="singleTop"
            android:name=".gamespace.ui.GameSpaceSplashActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/game_app_name"
            android:launchMode="singleTop"
            android:name=".gamespace.ui.GameSpaceHostActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:launchMode="singleTop"
            android:name=".gamespace.ui.GameSpaceUsageActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:configChanges="keyboardHidden"
            android:launchMode="singleTop"
            android:name=".gamespace.ui.PrivacySettingsActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing" />

        <!-- ============================================================ -->
        <!--  NEW ACTIVITIES — ajout jeu & collection                     -->
        <!-- ============================================================ -->

        <!-- Add Game Activity: sélectionner une app installée -->
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:name=".gamespace.ui.add.AddGameActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing"
            android:exported="false" />

        <!-- Add Collection Activity: importer depuis fichier metadata Pegasus -->
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:name=".gamespace.ui.collection.AddCollectionActivity"
            android:screenOrientation="landscape"
            android:theme="@style/game_space_full_screen"
            android:windowSoftInputMode="adjustNothing"
            android:exported="false" />

        <!-- ============================================================ -->
        <!--  PROVIDERS                                                    -->
        <!-- ============================================================ -->
        <provider
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            android:name="androidx.core.content.FileProvider">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
